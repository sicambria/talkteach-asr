<!-- Recreation template for OpenAI Codex (native — no file emitted). -->
OpenAI Codex CLI reads `AGENTS.md` natively as its instruction file, so kaizen emits no
redundant overlay for it. The canonical contract in `AGENTS.md` is already the source of truth.
