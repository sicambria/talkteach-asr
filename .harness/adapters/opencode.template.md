<!-- Recreation template for opencode.jsonc (regenerate with kaizen init). -->
// Managed by kaizen. opencode loads these instruction files into context; AGENTS.md is the source of truth.
{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "AGENTS.md"
  ]
}
